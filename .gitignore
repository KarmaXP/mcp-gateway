# Binaries
bin/
# Root-level binary only (do not use bare "gateway" — it ignores internal/gateway/)
/gateway

# Dependencies
vendor/

# Environment variables and secrets
.env
.env.*
!.env.example

# IDEs and Editors
.vscode/
.idea/
.cursor/
.claude/
**/.claude/
*.swp

# Maintainer's local working notes and agent rules. Never published.
.personal/
CLAUDE.md

# Logs and temporary files
*.log
tmp/
.ci-bin/

# OS specific files
.DS_Store
Thumbs.db
# golangci-lint migrate leaves this behind
.golangci.bck.yml

# Binaries left by `go build ./cmd/<name>` in the repository root.
/gateway
/gen-jwt
/gen-router-eval-catalog
/loadtest
/mcp_host_demo
/mock_upstream
/smoke_upstream
