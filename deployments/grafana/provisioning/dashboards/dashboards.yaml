apiVersion: 1

providers:
  - name: "mcp-gateway"
    orgId: 1
    folder: "MCP Gateway"
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards
